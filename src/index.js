// Import components and utilities
import { initializeHeroVideo } from './js/heroVideo.js';

// Initialize all components when DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
    initializeHeroVideo();
});